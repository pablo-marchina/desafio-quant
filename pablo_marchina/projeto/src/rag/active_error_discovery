"""active error discovery

Hypothesis: Evaluate whether active error discovery improves final product output without paid dependency.
Category: 8.43 Memory and Negative Learning
Expected runtime use: candidate_or_supporting_governance
"""

from __future__ import annotations

from typing import Any

from src.rag.schemas import RetrievedContext


class ActiveErrorDiscovery:
    """active error discovery"""

    def __init__(self, config: Any | None = None) -> None:
        self.config = config or {}

    def run(self, contexts: list[RetrievedContext], **kwargs: Any) -> list[RetrievedContext]:
        return self._apply(contexts, **kwargs)

    def _apply(self, contexts: list[RetrievedContext], **kwargs: Any) -> list[RetrievedContext]:
        error_patterns = [
                    ("inconsistent", 0.1), ("contradict", 0.12), ("error", 0.08),
                    ("outdated", 0.1), ("deprecated", 0.08), ("incorrect", 0.1),
                    ("misleading", 0.12), ("inaccurate", 0.1),
                ]
                for ctx in contexts:
                    for pat, penalty in error_patterns:
                        if pat.lower() in ctx.content.lower():
                            ctx.relevance_score = max(0.0, ctx.relevance_score - penalty)
        return contexts
