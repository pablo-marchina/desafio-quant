# Orquestração / agentes
langgraph
langgraph-checkpoint-postgres
langchain
langchain-nvidia-ai-endpoints

# API / config / fila
fastapi
uvicorn[standard]
sse-starlette
pydantic>=2
pydantic-settings
redis
rq

# Dados
psycopg[binary]
sqlmodel
alembic
qdrant-client

# Scraping
tavily-python
firecrawl-py
playwright
trafilatura
beautifulsoup4
scrapy

# RAG / eval / guardrails
ragas
nemoguardrails
rank-bm25

# Briefing — saída PDF server-side (F4.6): puro-Python, offline, sem GPU; modo invariant
# (data/ID fixos) p/ run reprodutível. pypdf (já transitivo) só verifica o conteúdo nos testes.
reportlab

# Observabilidade
langfuse>=3  # servidor Langfuse v3 (SDK 4.x; langfuse.langchain.CallbackHandler) — F0.8

# Benchmark / GPU (instalar conforme CUDA; ver F6)
# cudf-cu12, cuml-cu12, tensorrt-llm  -> instalados no ambiente GPU
