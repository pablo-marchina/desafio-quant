# Deps p/ lint + testes no CI (F0.10), sem GPU. Os testes importam quase toda a stack via os
# __init__ de packages/: scraping puxa trafilatura/scrapy/playwright/bs4; rag puxa
# qdrant-client/numpy/rank-bm25; worker/api puxam redis/rq/psycopg. nemoguardrails fica DE FORA
# (import lazy, só no container — annoy/C++) e as deps de GPU (cudf/cuml/tensorrt) idem.

# Orquestração / agentes
langgraph
langgraph-checkpoint-postgres
langchain
langchain-nvidia-ai-endpoints

# API / config / fila
fastapi
uvicorn[standard]
sse-starlette
pydantic>=2
pydantic-settings
redis
rq

# Dados
psycopg[binary]
sqlmodel
alembic
qdrant-client
numpy

# Scraping
tavily-python
firecrawl-py
playwright
trafilatura
beautifulsoup4
scrapy

# RAG / eval (nemoguardrails NÃO — import lazy, só no container)
ragas
rank-bm25
cohere    # comparativo de reranker F7.4 (tests/test_rerank.py mockam o client; sem chave)

# Briefing / observabilidade / config
reportlab
langfuse>=3
pyyaml

# Ferramentas de CI
pytest
ruff
pypdf
httpx
