"""GPU Graduation Engine (F6.5-F6.12).

Coorte: cuDF (normalização/dedup) + cuML (clustering). Benchmark: Triton + TensorRT-LLM na GPU
local -> matriz (tokens/s, p50/p95, throughput) -> estimativa de ROI (API externa vs NIM).
"""
